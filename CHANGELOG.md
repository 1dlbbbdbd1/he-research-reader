# 版本记录

本文件只记录已经形成可安装或可携带交付物的里程碑版本。开发中的普通提交不单独发布 Release。

## 产品路线修订（2026-08-01，尚未发布）

- 依据 UPDF 官方当前功能页重新核对产品基准：阅读中 AI、多文件项目、语义搜索、工具导航式 Copilot、结构化书签，以及引用/相似性文献图谱均视为成熟产品的公共能力。
- 长期定位明确为“从单界面阅读闭环长出的本地优先、证据约束科研 Agent”，不扩展成通用 PDF 编辑器，也不堆叠彼此割裂的 Agent 按钮。
- 图谱拆分为引用图、证据图和想法图；近期优先证据图，复用已有来源、碎片、证据结论和复查数据。
- 路线调整为：补齐阅读器与批注 → 本地混合检索 → 持续上下文侧栏 → 证据图 → 受控 Agent 行动 → 外部论文发现与深度研究。
- 明确 Agent 权限：只读检索可直接执行，草稿可撤销；正式写入、联网、导出和批量操作必须由用户确认。
- 阅读器补齐本地 PDF 目录、按需缩略图、`Ctrl + F` 页内搜索，以及页码/模式/缩放恢复；真实两页 PDF 的搜索回跳和重开恢复已完成浏览器验收。
- 数据库升级至 schema v4：批注保存当前用户笔记版本指针，编辑会追加 `NoteFragment` 修订并保留 `supersedes_id` 历史；批注事件不可覆盖。
- 阅读侧栏增加批注编辑、归档、即时撤销和本篇 Markdown 导出；归档批注不再进入当前计数、检索、阅读卡或新复查材料，已有引用仍保留。
- 学术 Markdown 正文块增加稳定编号与当前版本指纹；划词批注保存块级锚点，PDF 引文仅在 Markdown 中唯一命中时定位和突出对应块，重复/缺失命中不猜测；页码只从明确页面标记继承。
- 按项目内 MinerU 3.4.4 真实产物和已安装源码接入 `*_content_list.json` 的 `text/page_idx/bbox`：`page_idx` 从 0 转为阅读页码，`bbox` 按源码定义从 0–1000 页面归一化坐标转成 PDF 比例矩形；越界、重复或无法唯一对应的框不生成高亮。
- 增加隔离桌面验收数据脚本：在项目缓存内创建独立用户数据、研究库、真实测试 PDF 和 MinerU 产物，不接触正式研究库；现已用真实鼠标完成 PDF 拖选、统一菜单、“添加到对话”、保存批注、页码/矩形高亮和重启恢复验收。
- 增加可选本地语义基础设施：FastEmbed 0.8.0（Apache-2.0）与 `BAAI/bge-small-zh-v1.5`（MIT、512 维）使用独立 Python/模型目录；安装阶段才联网，日常状态与嵌入强制读取本机缓存，并限制批量和字符数。
- 数据库升级至 schema v5：增加研究库语义索引状态和带题录、来源、页码、锚点的本地向量分块缓存；精确索引变化会立即使旧向量失效，生成期间内容变化会拒绝提交。
- 本地混合检索已接入研究 Agent 和设置页：可显式建立/更新当前研究库索引，FTS5 与语义结果通过倒数排名融合并去重；模型缺失或索引失败时保留精确检索并明确说明回退原因。
- 研究 Agent 从中央遮挡弹窗迁入阅读器右侧工作区；打开时自动收起左侧导航，证据回跳不关闭任务，可随时切回笔记/阅读卡。浏览器生产预览实测正文 850 px、侧栏 430 px且无横向溢出。
- 研究 Agent 增加选区、当前页、本篇、多篇和全库五档范围；选区/当前页直接复用阅读器现有页码与锚点，本篇/多篇/全库继续复用本地混合检索。当前窗口可连续追问，历史对话只用于消解追问，不作为答案证据，也没有新增未经确认的持久化会话模型。
- 划词菜单增加“添加到对话”：把临时选区固定为带页码/锚点的 Agent 证据并打开阅读侧栏；页面滚动不会丢失已加入内容，用户可从对话中移除，旧轮次仍保留各自的证据快照。
- Agent 侧栏改为浅色中性桌面层级，使用问题卡、证据脊线与可回跳引用片区区分用户问题、证据回答和来源；快捷问题在窄栏自动纵向排列，未引入新的 UI 框架。
- 产品显示名称统一为“小何的科研阅读助手”；内部应用 ID 与引用协议保持不变。正式品牌采用深海军蓝大写 `H`、紫色研究轨道和证据锚点，已交付彩色/纯色/单色 SVG、16–512 px PNG 与多尺寸 Windows ICO，并接入侧栏、网页页签、Electron 窗口和 Windows 打包配置。
- 品牌与发布链路接入通过 90 项测试与生产构建；常规侧栏及 900×700 折叠图标栏完成本地目视检查，无横向溢出或控制台问题。正式 H 图标已进入 0.2.0 安装版、便携版和可执行文件元数据，GitHub 工作流按新产物名收集安装版与便携版。
- Electron 隔离测试入口已规避受管 Windows 会话的 GPU/Renderer 子进程崩溃；无界面冒烟与真实交互窗口均成功启动并正常退出。Renderer 增加显式 Content Security Policy，不允许 `unsafe-eval`，同时保留用户主动配置的 HTTP/HTTPS Provider 连接。
- 新增真实研究库离线冒烟：临时库建立 6 个分块，相关装配证据相似度 `0.680135`，无关农业文本 `0.351769`；批注页码/矩形可追溯，重复证据同时标记精确与语义通道。
- 补齐标准 HTML 文档声明、中文语言和 viewport，消除 KaTeX 在 quirks mode 下的排版警告。
- 新增桌面视觉系统覆盖层：以 12px 常规说明和 14px/20px 正文为可读性基线，移除外部 Google Fonts 请求，并把旧版 90% 界面缩放钳制到 100%。
- 资料库、阅读器、复查文档和设置页改为固定窗口外壳与工作区内部滚动；按 1320/1080/920px 重新编排内容，900px 窄窗口折叠为 64px 图标栏，而不是整体缩小文字。生产预览已覆盖 900×700、1024×768 和 1600×900，并已进入当前安装包。
- 新增证据关系投影与“证据脊柱”界面：复用 `fragment_relations`、`review_blocks`、`review_citations`，区分原文、用户笔记、AI 草稿、已采纳 AI、复查结论和无来源待核验；支持整库/单篇/复查范围、用户/AI/支持反驳筛选，以及原文和复查回跳。没有新增核心业务模型，也不会通过文本相似度暗猜证据关系。
- 数据库升级至 schema v6：`fragment_relations` 增加创建者、审核状态、判断理由和审核时间；新增只追加的 `fragment_relation_events` 审计表，拒绝或撤销关系不物理删除历史。
- 证据节点详情增加内联关系编辑：用户只能建立“支持、反驳、补充”并必须填写理由；AI 语义建议可采纳或拒绝，已确认人工关系可撤销；“批注于、整理依据、引用依据”继续由原工作流维护，不能在关系页伪造。
- 明确视觉重构只是贯穿所有页面的横向质量门槛；真实桌面划词与 `bbox`、新版安装包和全新安装/卸载已验收，后续回到受控 Agent 与跨论文证据排序。
- 数据库升级至 schema v7：新增 `ActionPack`、行动项、不可覆盖的证据快照和只追加审批事件；AI 只能提出六类带白名单引用的行动，用户逐条确认、拒绝或标记完成，确认本身不执行任何外部操作。
- 新增研究任务单式“行动建议”页面；ActionPack 基础代码已接入，但按恢复后的路线暂停继续扩张，先完成桌面阅读现场与安装包验收。
- 0.2.0 新安装版与便携版已从当前源码重建；包内核对确认产品名、版本、CSP、正式 H 品牌、“添加到对话”和固定选区证据均存在。
- 在同一 Windows 用户上下文完成安装、内置 Argos 英译中、启动、正常退出和卸载回归；卸载后安装目录、文件、注册项、桌面与开始菜单快捷方式均为零残留。安装器额外清理会处理 Argos/Stanza 首次运行改写的组件文件，但不删除用户研究库或用户数据。

## 0.2.0

- 资料库升级为论文项目管理页，显示阅读阶段、页码进度、批注数、相关性、研究用途、附件与 MinerU 状态。
- 复查材料选择器增加论文状态、阅读进度、批注数量和批注全选/清空。
- 阅读器增加 `Ctrl + 滚轮`缩放、原文取证式批注、论文/附件/页码来源记录与 Windows 菜单栏移除。
- MinerU Markdown 使用 React Markdown、GFM 和 KaTeX 形成学术阅读层，并绑定题录、作者、年份、DOI 与关键词。
- MinerU 完整输出、Markdown、图片与逐文件哈希清单按论文和解析版本保存在研究库中；重启后可恢复，Markdown 图片只从受控本地资源接口加载。
- 新增受限 Markdown 章节识别工作流：AI 只能返回章节边界，不能改写或覆盖原文。
- 研究 Agent 接入本篇、多篇和全库作用域的本地检索、证据排序、引用白名单与原文回跳。
- 单篇阅读卡接入题录、阅读状态、原文证据、用户笔记和可选派生正文；AI 结果先保存为可追溯草稿，用户采纳后才成为当前阅读卡，且服务层阻止把用户笔记冒充论文结论。
- 设置中心增加界面缩放、信息密度、底色、强调色、Markdown 字号、行距和正文宽度。
- 桌面 AI 配置改为本机持久化；API 密钥使用 Electron 系统加密，无法加密时拒绝明文保存。
- Windows 安装器支持选择是否安装离线 Argos 翻译模块；未安装时可在应用内安装或配置 API。

已知限制：

- Windows 产物仍未代码签名，首次运行可能出现 SmartScreen 提示。
- MinerU 派生版本比较/清理界面和 span 级文字定位仍待实现；当前已完成保守的 Markdown 块级锚点、页码和矩形映射。
- 研究 Agent 已使用本地精确 + 语义混合检索并支持当前窗口多轮追问；PaperQA2 式候选证据重排和跨重启会话持久化尚未接入。
- PDF.js、React Markdown 与 KaTeX 仍在主阅读包中，后续需要按阅读模式动态加载。

## 0.1.0

- 建立 Electron Windows 桌面客户端、研究库与 SQLite 数据底座。
- 支持 PDF 阅读、文字层、批注锚点、阅读状态和本地精确检索。
- 支持 EndNote XML、RIS、BibTeX 导入与旧数据幂等迁移。
- 接入本地 MinerU 解析和 Argos 英译中。
- 支持多文献复查、引用回跳、Markdown/Word 导出。
- 提供 x64 Windows 安装版和免安装便携版。

已知限制：

- Windows 产物尚未代码签名，首次运行可能出现 SmartScreen 提示。
- PDF.js 主包仍有体积警告，后续需要继续拆分加载。
- MinerU 图片、布局与完整产物目录的版本化仍待接入。
